# PhageMine v1.2 development environment

- Workspace: `/Users/emmanuelnnadi/Documents/PhageMine_v1.2_development`
- Branch: `develop/v1.2`
- Starting and current source SHA: `610cdc0f79ed826d7f526c4aa947a953e19556ad`
- Conda environment: `phagemine_v12_dev` (cloned from the validated 3.12 benchmark environment, then repaired with an editable install)
- Python: 3.12.14
- pip: 26.2.1
- PhageMine: 1.1.0, editable install from this checkout
- Imported module: `src/phagemine/__init__.py` in this checkout
- CLI: `/opt/anaconda3/envs/phagemine_v12_dev/bin/phagemine`, version 1.1.0

## Baseline

The initial shell-Python run failed at collection with `ModuleNotFoundError`
because the `src` package was not installed and `PYTHONPATH` was not set. After
creating the dedicated environment and installing the checkout editable, the
documented baseline command `python -m pytest -q` passed:

`204 passed, 10 deselected in 54.75s` (0 failed; no skips reported).

Complete output is preserved in `docs/v1.2-baseline-pytest.txt`.

## External executable provenance

The dedicated environment resolves: `phanotate.py` 1.6.7,
Prodigal 2.6.3, HMMER/hmmscan 3.4, MMseqs2 18.8cc5c, DIAMOND 2.2.5,
Mash 2.3, and BLAST+ 2.17.0. Paths and command output are preserved in
`docs/v1.2-external-tools.txt`.

Step 3/6 native provider packages are `pyrodigal==3.7.1` and
`pyrodigal-gv==0.3.2`; their reproducible pip pins are listed in
`environment/v1.2-dev-pip-requirements.txt`.

Step 8 adds the RNA provider pin `pyrodigal-rv==0.1.0`. It is installed and
imports successfully alongside `pyrodigal==3.7.1` and
`pyrodigal-gv==0.3.2` in the development environment.

No PhageMine source files or tests were modified. This environment is ready for
v1.2 implementation, subject to the separately documented design review.
